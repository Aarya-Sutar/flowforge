[pytest]
pythonpath = .
markers =
    integration: requires a live Docker Compose stack (postgres, redis, celery-worker, backend) reachable at INTEGRATION_API_URL. Auto-skips with a clear reason if the stack isn't up. Run explicitly with: pytest tests/integration
